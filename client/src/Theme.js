import { createTheme } from '@mui/material/styles';

const mainTheme = createTheme({
  palette: {
    primary: {
      main: '#1F8FDE'
    },
    success: {
        main: '#0FBB53'
    },
    dark: {
      main: '#2F3133'
  },

  },
  });

export default mainTheme