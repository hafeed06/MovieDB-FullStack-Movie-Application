import { createTheme } from '@mui/material/styles';

const mainTheme = createTheme({
  palette: {
    primary: {
      main: '#1F8FDE'
    },
    success: {
        main: '#14b817'
    },
    dark: {
      main: '#FFFFFF'
  },

  },
  });

export default mainTheme