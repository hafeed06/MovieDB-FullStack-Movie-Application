pipeline {
  agent any {
    stages {
      stage("build") {
        steps {
          echo 'Testing Jenkins Build'
        }
      }
      stage("test") {
        steps {
          echo 'Testing Jenkins Test'
        }
      }
      stage("deploy") {
        steps {
          echo 'Deploying the application'
        }
      }
    }
  }
}
